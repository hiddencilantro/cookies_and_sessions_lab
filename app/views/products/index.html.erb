<ul>
    <% cart.each do |item| %>
        <li><%= item %></li>
    <% end %>
</ul>

<%= form_tag controller: "products", action: "index", method: "post" do %>
    <%= label_tag :product, "Name of product:" %>
    <%= text_field_tag :product %>
    <%= submit_tag "add to cart" %>
<% end %>
